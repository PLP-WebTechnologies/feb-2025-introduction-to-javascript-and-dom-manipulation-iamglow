body {
    font-family: Arial, sans-serif;
}

header, section, footer {
    margin: 20px;
}

button {
    padding: 10px;
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    background-color: #f0f0f0;
}
